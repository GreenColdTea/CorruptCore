package psych.ui;

import flixel.FlxSprite;
import flixel.group.FlxSpriteGroup;

class PsychUIGroup extends FlxSpriteGroup
{
    public function new(x:Float = 0, y:Float = 0)
    {
        super(x, y); // prob nothing for now
    }
}